version: "3.9"

services:
  redis:
    image: "redis:6.2.6-alpine"
    restart: unless-stopped

  web0:
    image: "local/redis_cache_example_web"
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/code
    ports:
      - "5000:5000"

  web1:
    image: "local/redis_cache_example_web"
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/code
    ports:
      - "5001:5000"

networks:
  default:
    name: my-compose-network
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.80.0.0/24